# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# PyInstaller
build/
dist/
*.spec

# Dictionary build intermediates (the final glance-dict.db IS committed)
stardict.7z
stardict.csv
ecdict*.csv

# App runtime
config.json

# Local-only project docs — kept out of the public repo, backed up to the NAS
# mirror by sync-to-nas.bat (see KEEP_LOCAL in sync_to_nas.py)
AGENTS.md
CLAUDE.md
DECISIONS.md
.claude/

# NAS mirror marker (written by sync-to-nas.bat, only exists on the mirror)
MIRROR.txt

# Dev scratch  (docs/preview.png is committed and lives under docs/, not here)
/demo.png
/demo_*.png
/preview.png
_*.py
*.tmp
